import { CommonContainerDirective } from './common-container.directive';

describe('CommonContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonContainerDirective();
    expect(directive).toBeTruthy();
  });
});
