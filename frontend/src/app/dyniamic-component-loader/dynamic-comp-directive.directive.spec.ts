import { DynamicCompDirectiveDirective } from './dynamic-comp-directive.directive';

describe('DynamicCompDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicCompDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
